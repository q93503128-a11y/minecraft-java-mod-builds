# AGENTS.md

이 파일은 이 저장소를 다루는 ChatGPT, Codex, Claude, Gemini, 기타 자동화 에이전트를 위한 작업 계약이다.

## 저장소 정체성

이 저장소는 여러 Minecraft Java 모드를 실제로 컴파일·검사·패키징하기 위한 공개 멀티프로젝트 빌드 저장소다.

단순 정적 검토나 소스 재포장이 목적이 아니다. 최종 목표는 사용자가 Minecraft 런처 또는 Modrinth App에 직접 넣을 수 있는 **실행용 JAR**을 만드는 것이다.

## 작업 시작 시 필수 행동

1. `README.md`와 `docs/BUILD_STANDARD.md`를 먼저 읽는다.
2. 대상 프로젝트의 `PROJECT.md`, README, CHANGELOG, Gradle 파일, Java 소스, 리소스, 도구를 모두 읽는다.
3. 최신 기준 파일이나 ZIP이 제공된 경우 직접 압축 해제한 결과를 절대 기준으로 사용한다.
4. 현재 요구 버전이 실제 공식 Maven과 공식 문서에 존재하는지 확인한다.
5. 대상 프로젝트 외의 다른 모드 폴더를 수정하지 않는다.

## 브랜치와 커밋 정책

- 기본적으로 `main`에 직접 작업한다.
- 사용자가 명시하지 않은 브랜치, PR, 임시 fork를 만들지 않는다.
- 한 번의 커밋은 한 프로젝트 또는 한 목적에 집중한다.
- 빌드 실패 수정은 로그의 가장 첫 원인을 기준으로 작은 단위로 진행한다.
- 기존 기록을 지우거나 force push하지 않는다.

## 구현 보존 원칙

절대 금지:

- 기존 기능을 삭제해 빌드만 통과시키기
- 대량 주석 처리
- 임시 빈 구현이나 no-op 스텁으로 오류 숨기기
- 가짜 Minecraft/NeoForge/Fabric API 클래스 추가
- 기존 mod id, 등록 ID, 네임스페이스, 저장 키 변경
- 사용자 허락 없이 기존 월드 호환성을 깨는 데이터 구조 변경
- 소스 ZIP의 확장자만 `.jar`로 바꾸기
- `.class`가 없는 빈 JAR 제출

API 변경으로 기존 코드를 그대로 유지할 수 없는 경우, 기능 의도를 보존하는 공식 최신 API 구현으로 마이그레이션하고 그 근거를 보고서에 기록한다.

## 실제 빌드 계약

최소 실행 순서:

1. Java와 Gradle 버전 확인
2. `clean build`
3. 컴파일 실패 시 가장 첫 원인 수정
4. 성공할 때까지 반복
5. 프로젝트가 지원하면 datagen 실행
6. 프로젝트가 지원하면 GameTest 실행
7. 가능한 경우 전용 서버 최소 부팅
8. 가능한 경우 클라이언트 최소 로딩
9. 최종 JAR 내부 검사
10. SHA-256 생성
11. 결과 artifact 업로드

빌드, datagen, GameTest, 서버, 클라이언트 중 실행하지 못한 항목은 반드시 `NOT RUN` 또는 `BLOCKED`로 적는다. 실행하지 않은 작업을 성공으로 표시하지 않는다.

## 로그 처리

- 전체 원본 로그를 보존한다.
- 사용자명, 로컬 경로, 토큰, 쿠키, 계정 정보가 로그에 포함되면 공개 업로드 전에 제거하거나 마스킹한다.
- 요약 보고서에는 첫 원인 오류, 수정 내용, 남은 경고, 실행한 명령과 결과를 기록한다.
- 실패 시에도 로그 artifact를 남겨 다음 작업자가 바로 이어서 수정할 수 있게 한다.

## 공개 저장소 보안

다음은 절대 커밋하지 않는다.

- `.env` 및 환경별 비밀 설정
- GitHub token, API key, 비밀번호
- 인증서, SSH 키, 쿠키, 브라우저 프로필
- 개인 정보가 포함된 로그
- 배포 권한이 없는 리소스
- Minecraft 원본 배포 파일

비밀이 이미 커밋된 흔적을 발견하면 즉시 작업을 멈추고 사용자에게 알린다. 파일 삭제만으로 해결됐다고 주장하지 않는다.

## 프로젝트 격리

모든 모드는 `projects/<slug>/` 아래에 둔다.

- 소스, wrapper, 리소스, datagen, 테스트는 프로젝트 폴더 내부에 둔다.
- workflow는 프로젝트별로 분리한다.
- workflow의 `working-directory`를 명시한다.
- artifact 이름에 프로젝트명과 버전을 포함한다.
- 한 프로젝트 실패가 다른 프로젝트 빌드를 막지 않게 한다.

## JAR 검증 계약

최종 JAR은 ZIP으로 열어 최소한 다음을 확인한다.

- 로더 메타데이터 존재
- 실제 컴파일된 `.class` 존재
- `assets/<modid>` 존재
- `data/<modid>` 존재
- 개발 전용 소스와 문서 미포함
- 중복 ZIP entry 없음
- 파일 크기 0이 아님
- manifest와 버전 정보가 프로젝트 설정과 일치

검사 스크립트가 있으면 실행하고, 없으면 재현 가능한 검사 스크립트를 추가한다.

## 결과 전달

성공 시 최소 결과물:

```text
<mod>-<version>.jar
<mod>-<version>.jar.sha256
BUILD_AND_RUNTIME_REPORT.md
raw-logs.zip
```

지원되는 경우 `.mrpack`도 제공한다.

최종 응답에서는 실제 검증된 JAR을 가장 먼저 제시한다. 실패했다면 JAR이 있다고 꾸미지 말고 첫 원인 오류와 로그를 제시한다.

## 현재 저장소에서의 기본 판단

- 저장소는 공개 빌드 허브다.
- 공개 표준 GitHub Actions runner를 정상적인 모드 개발·테스트 용도로 사용한다.
- 한 저장소에 여러 모드를 넣어도 되지만 프로젝트 폴더와 workflow는 분리한다.
- 새 작업자가 저장소 주소만 받으면 추가 설명을 기다리지 말고 문서와 대상 폴더를 먼저 검사한다.
