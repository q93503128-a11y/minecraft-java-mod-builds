name: Audit Living Kingdoms exterior residence hamlets

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'projects/living-kingdoms/src/main/java/kr/moonseungjun/livingkingdoms/world/ErdenExteriorResidenceCatalog.java'
      - 'projects/living-kingdoms/src/main/java/kr/moonseungjun/livingkingdoms/world/ErdenExteriorResidenceBuilder.java'
      - 'projects/living-kingdoms/src/main/java/kr/moonseungjun/livingkingdoms/world/ErdenKingdomSupplyCatalog.java'
      - '.github/scripts/audit_living_kingdoms_exterior_residence_overlap.py'
      - '.github/scripts/audit_living_kingdoms_exterior_residence_hamlets.py'
      - '.github/workflows/audit-living-kingdoms-exterior-residence-overlap.yml'

concurrency:
  group: living-kingdoms-exterior-residence-hamlets-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Prove all 74 physical worker homes avoid structures, worksites and freight roads
        run: python .github/scripts/audit_living_kingdoms_exterior_residence_hamlets.py
      - name: Upload hamlet collision report
        id: upload
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: living-kingdoms-exterior-residence-hamlets-${{ github.run_number }}
          path: logs/exterior-residence-hamlets.json
          if-no-files-found: warn
          retention-days: 30
      - name: Record hamlet collision audit pointer
        if: always()
        env:
          JOB_STATUS: ${{ job.status }}
          LOG_ARTIFACT_ID: ${{ steps.upload.outputs.artifact-id }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          for attempt in 1 2 3 4 5; do
            git fetch origin main
            git checkout --detach origin/main
            mkdir -p .github/build-pointers
            cat > .github/build-pointers/living-kingdoms-exterior-residence-overlap.json <<EOF
          {
            "run_id": "$GITHUB_RUN_ID",
            "run_number": "$GITHUB_RUN_NUMBER",
            "commit_sha": "$GITHUB_SHA",
            "job_status": "$JOB_STATUS",
            "log_artifact_id": "$LOG_ARTIFACT_ID"
          }
          EOF
            git add .github/build-pointers/living-kingdoms-exterior-residence-overlap.json
            git commit -m "Record Living Kingdoms residence hamlet audit $GITHUB_RUN_NUMBER" || exit 0
            if git push origin HEAD:main; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
