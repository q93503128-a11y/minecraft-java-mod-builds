name: Survival Risk Fix Trigger
# trigger v2

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/zz-survival-risk-fix-trigger.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
