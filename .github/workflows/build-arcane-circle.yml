name: Build Arcane Circle

on:
  push:
    branches: [main]
    paths:
      - '.github/arcane-alpha46-ready'

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'alpha.46 staging paused while source payload is replaced safely'
