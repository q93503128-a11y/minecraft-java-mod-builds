name: Build Arcane Circle

on:
  push:
    branches: [main]
    paths:
      - 'projects/arcane-circle/**'
      - '.github/workflows/build-arcane-circle.yml'

permissions:
  contents: write

concurrency:
  group: arcane-circle-main
  cancel-in-progress: true

jobs:
  migrate:
    name: One-shot alpha.45 audit correction
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Patch audit expressions only
        shell: bash
        run: |
          set -euo pipefail
          test -f .github/scripts/fix-arcane-audit45.py
          python3 .github/scripts/fix-arcane-audit45.py
