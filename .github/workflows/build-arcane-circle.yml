name: Build Arcane Circle

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Arcane Circle v0.4 source replacement in progress.'
