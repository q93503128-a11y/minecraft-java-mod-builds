name: Build Arcane Circle

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Arcane Circle v0.12 world-geometry overhaul is being assembled.'
