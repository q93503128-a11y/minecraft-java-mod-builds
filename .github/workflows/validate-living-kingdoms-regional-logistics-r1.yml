name: Validate Living Kingdoms regional logistics r1

on:
  pull_request:
    branches: [main]
    paths:
      - 'projects/living-kingdoms/**'
      - '.github/workflows/validate-living-kingdoms-regional-logistics-r1.yml'

concurrency:
  group: living-kingdoms-regional-logistics-r1-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  PROJECT_DIR: projects/living-kingdoms
  LIVING_KINGDOMS_CI_REALM_TEST: '1'
  LIVING_KINGDOMS_CI_CLIENT_TEST: '1'

jobs:
  validate:
    name: Java 25 / regional roads / freight / client / JAR
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.2.1'

      - name: Java 25 clean build
        working-directory: ${{ env.PROJECT_DIR }}
        run: gradle --no-daemon clean build --stacktrace --console=plain

      - name: Fresh dedicated server regional logistics audit
        working-directory: ${{ env.PROJECT_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ../../logs run
          rm -rf run/world run/living_realm run/livingkingdoms-regional-logistics-ci-world
          printf 'level-name=livingkingdoms-regional-logistics-ci-world\npause-when-empty-seconds=0\n' > run/server.properties
          printf 'eula=true\n' > run/eula.txt
          gradle --no-daemon runServer --stacktrace --console=plain > ../../logs/regional-logistics-server.log 2>&1 &
          pid=$!
          trap 'kill "$pid" 2>/dev/null || true; wait "$pid" 2>/dev/null || true' EXIT
          passed=0
          for attempt in $(seq 1 1400); do
            if grep -Eq 'LK_REALM_DIAGNOSTIC_FAIL|Exception in server tick loop|watchdog|OutOfMemoryError|NoSuchMethodError|NoClassDefFoundError' ../../logs/regional-logistics-server.log; then
              cat ../../logs/regional-logistics-server.log
              exit 1
            fi
            if grep -Fq 'LK_ERDEN_REGIONAL_ROADS_PASS revision=1 corridors=8 waystations=4' ../../logs/regional-logistics-server.log \
              && grep -Fq 'LK_ERDEN_REGIONAL_LOGISTICS_PASS revision=1 corridors=8 waystations=4' ../../logs/regional-logistics-server.log \
              && grep -Fq 'LK_ERDEN_REGIONAL_SETTLEMENT_PASS revision=1 settlements=6 buildings=60' ../../logs/regional-logistics-server.log \
              && grep -Fq 'LK_ERDEN_REGIONAL_SOCIETY_PASS revision=1 settlements=6 households=48 residents=144 workers=96 dependents=48' ../../logs/regional-logistics-server.log \
              && grep -Fq 'LK_ERDEN_REGIONAL_ECONOMY_PASS revision=1 settlements=6 households=48 markets=6' ../../logs/regional-logistics-server.log \
              && grep -Fq 'LK_ERDEN_POPULATION_DIAGNOSTIC_PASS households=77 residents=231 workers=154' ../../logs/regional-logistics-server.log \
              && grep -Fq 'LK_ERDEN_PHYSICAL_ECONOMY_PASS sites=156 warehouses=15 wallets=77' ../../logs/regional-logistics-server.log \
              && grep -Fq 'LK_ERDEN_LIVING_ECONOMY_PASS revision=1 households=77 shops=50' ../../logs/regional-logistics-server.log \
              && grep -Fq 'LK_ERDEN_TRANSPORT_PASS revision=2 manifests=' ../../logs/regional-logistics-server.log \
              && grep -Fq 'LK_ERDEN_EXTERIOR_WORKFORCE_PASS revision=1 nodes=18 households=74 residents=216 workers=142 dependents=74' ../../logs/regional-logistics-server.log; then
              passed=1
              break
            fi
            if ! kill -0 "$pid" 2>/dev/null; then
              cat ../../logs/regional-logistics-server.log
              exit 1
            fi
            sleep 1
          done
          cat ../../logs/regional-logistics-server.log
          test "$passed" -eq 1
          grep -F 'LK_ERDEN_REGIONAL_ROADS_PASS revision=1 corridors=8 waystations=4' ../../logs/regional-logistics-server.log | grep -F 'network_connected=true' | grep -F 'streamed=true' | grep -F 'terrain_following=true' | grep -F 'physical_road=true' | grep -F 'physical_waystation=true' | grep -F 'persistent_forced_chunks=false'
          grep -F 'LK_ERDEN_REGIONAL_LOGISTICS_PASS revision=1 corridors=8 waystations=4' ../../logs/regional-logistics-server.log | grep -F 'capital_route=true' | grep -F 'authoritative_escrow=true' | grep -F 'loaded_projection=true' | grep -F 'reobservation_resync=true' | grep -F 'navigation_only=true' | grep -F 'observed_blockage_delays_or_returns=true' | grep -F 'aggregate_when_unloaded=true' | grep -F 'return_accounting=true' | grep -F 'persistent_forced_chunks=false'
          grep -F 'LK_ERDEN_REGIONAL_ECONOMY_PASS revision=1 settlements=6 households=48 markets=6' ../../logs/regional-logistics-server.log | grep -F 'physical_barrel=true' | grep -F 'worker_linked=true' | grep -F 'household_consumption=true' | grep -F 'local_trade_escrow=true' | grep -F 'kingdom_supply_escrow=true' | grep -F 'unloaded_aggregate=true' | grep -F 'persistent_forced_chunks=false'

      - name: Graphical client audit
        working-directory: ${{ env.PROJECT_DIR }}
        shell: bash
        env:
          LIBGL_ALWAYS_SOFTWARE: '1'
        run: |
          set -euo pipefail
          log=../../logs/regional-logistics-client.log
          : > "$log"
          need_graphics=0
          command -v xvfb-run >/dev/null 2>&1 || need_graphics=1
          ldconfig -p 2>/dev/null | grep -Fq 'libGL.so.1' || need_graphics=1
          if [ "$need_graphics" -eq 1 ]; then
            sudo timeout --signal=TERM --kill-after=10s 120s apt-get -o Acquire::Retries=2 -o Acquire::http::Timeout=20 -o Acquire::https::Timeout=20 update -qq
            sudo timeout --signal=TERM --kill-after=10s 120s apt-get -o Acquire::Retries=2 -o Acquire::http::Timeout=20 -o Acquire::https::Timeout=20 install -y --no-install-recommends xvfb libgl1-mesa-dri >/dev/null
          fi
          setsid bash -c "exec xvfb-run -a -s '-screen 0 1280x720x24' gradle --no-daemon runClient --stacktrace --console=plain" > "$log" 2>&1 &
          launcher=$!
          sleep 1
          pgid="$(ps -o pgid= -p "$launcher" 2>/dev/null | tr -d ' ' || true)"
          [ -n "$pgid" ] || pgid="$launcher"
          outcome=timeout
          for attempt in $(seq 1 240); do
            if grep -Fq 'LK_CLIENT_DIAGNOSTIC_PASS screen=origin_selection fixed_erden_origin=true' "$log" \
              && grep -Fq 'LK_CLIENT_LOADING_DIAGNOSTIC_PASS screen=realm_loading non_pausing=true' "$log" \
              && grep -Fq 'LK_CLIENT_CODEX_DIAGNOSTIC_PASS screens=overview,equipment,map,skills' "$log"; then outcome=pass; break; fi
            if grep -Eqi 'Caught exception from Living Kingdoms|Error during client initialization|Exception in client tick loop|Rendering screen|Missing textures in model livingkingdoms|Could not initialize GLFW|Failed to create window' "$log"; then outcome=fatal; break; fi
            if ! kill -0 "$launcher" 2>/dev/null; then outcome=client_exit; break; fi
            sleep 1
          done
          kill -TERM -- "-$pgid" 2>/dev/null || true
          sleep 3
          kill -KILL -- "-$pgid" 2>/dev/null || true
          wait "$launcher" 2>/dev/null || true
          if grep -Fq 'LK_CLIENT_DIAGNOSTIC_PASS screen=origin_selection fixed_erden_origin=true' "$log" \
            && grep -Fq 'LK_CLIENT_LOADING_DIAGNOSTIC_PASS screen=realm_loading non_pausing=true' "$log" \
            && grep -Fq 'LK_CLIENT_CODEX_DIAGNOSTIC_PASS screens=overview,equipment,map,skills' "$log"; then outcome=pass; fi
          cat "$log"
          test "$outcome" = pass

      - name: JAR verification
        working-directory: ${{ env.PROJECT_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t jars < <(find build/libs -maxdepth 1 -type f -name '*.jar' ! -name '*-sources.jar' ! -name '*-javadoc.jar' | sort)
          test "${#jars[@]}" -eq 1
          unzip -t "${jars[0]}"
          entries="$(mktemp)"
          unzip -Z1 "${jars[0]}" > "$entries"
          test "$(sort "$entries" | uniq -d | wc -l)" -eq 0
          grep -Fx 'kr/moonseungjun/livingkingdoms/world/ErdenRegionalRoadNetwork.class' "$entries"
          grep -Fx 'kr/moonseungjun/livingkingdoms/world/ErdenRegionalRoadSavedData.class' "$entries"
          grep -Fx 'kr/moonseungjun/livingkingdoms/world/ErdenRegionalRoadBuilder.class' "$entries"
          grep -Fx 'kr/moonseungjun/livingkingdoms/world/ErdenRegionalRoadManager.class' "$entries"
          grep -Fx 'kr/moonseungjun/livingkingdoms/world/ErdenRegionalTransportManager.class' "$entries"
          grep -Fx 'kr/moonseungjun/livingkingdoms/world/ErdenRegionalShipmentClockGuard.class' "$entries"
          grep -Fx 'kr/moonseungjun/livingkingdoms/world/ErdenTransportSavedData.class' "$entries"
          unzip -p "${jars[0]}" META-INF/neoforge.mods.toml | grep -F 'version="0.1.0-alpha.12"'

      - name: Upload regional logistics logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: living-kingdoms-regional-logistics-${{ github.run_number }}
          path: logs/
          if-no-files-found: warn
          retention-days: 14
