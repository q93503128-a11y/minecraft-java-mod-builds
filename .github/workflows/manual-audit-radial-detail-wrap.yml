name: Manual Audit Radial Detail Wrap

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/manual-audit-radial-detail-wrap.yml'
      - 'projects/survival-ascension/tools/patch_radial_detail_wrap.py'

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Apply radial detail wrap
        run: python3 projects/survival-ascension/tools/patch_radial_detail_wrap.py

      - name: Build Survival Ascension
        working-directory: projects/survival-ascension
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon --no-configuration-cache clean build --console=plain

      - name: Commit validated UI fix
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add projects/survival-ascension/src/main/java/kr/moonseungjun/survivalascension/client/InfrastructureRadialMenuScreen.java
          git add projects/survival-ascension/src/main/java/kr/moonseungjun/survivalascension/client/ProductionRadialMenuScreen.java
          if git diff --cached --quiet; then
            echo 'No UI source changes to commit.'
            exit 0
          fi
          git commit -m 'Wrap long shared economy radial details'
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
