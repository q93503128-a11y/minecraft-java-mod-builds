name: Frontier Alpha108 Tree Site 20260905

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/frontier-alpha108-tree-site-20260905.yml'

permissions:
  contents: write

jobs:
  patch-build-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
      - name: Apply Alpha108 patch
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('tools/apply_frontier_alpha108_tree_site_tmp.py')
          text = p.read_text(encoding='utf-8')
          text = text.replace('lock.get("versions", {}).get("frontier_settlement")', 'lock.get("target", {}).get("frontier_settlement")')
          text = text.replace('lock["versions"]["frontier_settlement"]', 'lock["target"]["frontier_settlement"]')
          p.write_text(text, encoding='utf-8')
          PY
          python3 tools/apply_frontier_alpha108_tree_site_tmp.py
      - name: Verify current source and JSON
        run: |
          cd projects/frontier-settlement
          python3 tools/test_current_source.py
          python3 - <<'PY'
          import json
          from pathlib import Path
          files=list(Path('src/main/resources').rglob('*.json'))
          for p in files:
              json.loads(p.read_text(encoding='utf-8'))
          print('FRONTIER JSON PASS', len(files))
          PY
      - name: Java 25 clean build
        run: |
          cd projects/frontier-settlement
          chmod +x gradlew
          ./gradlew --no-daemon clean build
      - name: Verify runtime JAR
        run: |
          python3 - <<'PY'
          import pathlib, zipfile
          folder=pathlib.Path('projects/frontier-settlement/build/libs')
          jars=[p for p in folder.glob('*.jar') if 'sources' not in p.name and 'javadoc' not in p.name]
          assert len(jars)==1, jars
          jar=jars[0]
          assert '0.1.0-alpha.108' in jar.name, jar
          with zipfile.ZipFile(jar) as z:
              names=z.namelist()
              assert len(names)==len(set(names))
              assert any(n.endswith('SettlementConstructionService.class') for n in names)
              assert 'META-INF/neoforge.mods.toml' in names
          print('JAR PASS', jar)
          PY
      - name: Commit canonical source and remove temporary files
        run: |
          git config user.name 'coworkaccount'
          git config user.email 'cyp1jhh2@gmail.com'
          git rm -f .github/workflows/frontier-alpha108-tree-site-20260905.yml tools/apply_frontier_alpha108_tree_site_tmp.py
          git add projects/frontier-settlement
          git commit -m 'Fix Frontier natural-site building placement'
      - name: Rebase and reverify current main
        run: |
          git fetch origin main
          git rebase origin/main
          cd projects/frontier-settlement
          python3 tools/test_current_source.py
          ./gradlew --no-daemon build
      - name: Stage artifact
        run: |
          mkdir -p dist
          cp projects/frontier-settlement/build/libs/frontier_settlement-0.1.0-alpha.108.jar dist/
      - uses: actions/upload-artifact@v4
        with:
          name: frontier-settlement-0.1.0-alpha.108-tested
          path: dist/*.jar
          if-no-files-found: error
      - name: Push main
        run: git push origin HEAD:main
