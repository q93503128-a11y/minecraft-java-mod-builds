name: Countryside Days source update paused

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary source applier disabled"
