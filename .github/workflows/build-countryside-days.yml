name: Build Countryside Days

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/build-countryside-days.yml'

permissions:
  contents: read

jobs:
  source-update-in-progress:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Countryside Days alpha.14 source update in progress"
