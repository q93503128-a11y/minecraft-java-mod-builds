name: Arcane Circle alpha.8 staging pause

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Arcane Circle alpha.8 files are being staged; automatic builds resume in the final workflow commit.'
