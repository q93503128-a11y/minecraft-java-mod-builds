name: Stage Arcane Circle v0.12.1-alpha.4

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Arcane Circle alpha.4 staging workflow; automatic builds resume after the atomic patch is complete.'
