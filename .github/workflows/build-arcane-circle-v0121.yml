name: Arcane Circle visual rewrite staging

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Arcane Circle alpha.26 visual rewrite staging; canonical build resumes with final workflow.'
