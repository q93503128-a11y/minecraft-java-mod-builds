name: Stage Arcane Circle v0.12.1-alpha.5

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Arcane Circle alpha.5 hierarchy, affiliation and aggro overhaul staging.'
