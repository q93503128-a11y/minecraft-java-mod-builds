name: Build Arcane Circle v0.12.1 Particle-Free Core

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  parked:
    name: Arcane Circle source update gate
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Arcane Circle automatic build is temporarily parked during an atomic source update.'
