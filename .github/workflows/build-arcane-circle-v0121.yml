name: Stage Arcane Circle v0.12.1-alpha.4 Economy Finalization

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Finalizing the persistent Arcana economy before the release rebuild.'
