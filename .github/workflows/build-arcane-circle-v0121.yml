name: Build Arcane Circle v0.12.1 Alpha.9 HUD Gear Kinetics

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'projects/arcane-circle/**'
      - '.github/workflows/build-arcane-circle-v0121.yml'

permissions:
  contents: write

concurrency:
  group: arcane-circle-v0121-main
  cancel-in-progress: true

env:
  PROJECT_DIR: projects/arcane-circle
  MOD_VERSION: 0.12.1-alpha.9
  EXPECTED_JAR: arcanecircle-0.12.1-alpha.9.jar
  PUBLISHED_DIR: published/arcane-circle
  CI_RESULT_DIR: ci-results/arcane-circle

jobs:
  build:
    name: Java 25 - compact vitality, detailed gear and spell kinetics
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.2.1'
          cache-read-only: false

      - name: Install alpha.9 HUD gear and kinetics
        working-directory: ${{ env.PROJECT_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/apply_v0121_alpha9_hud_gear_kinetics.py 2>&1 | tee "$GITHUB_WORKSPACE/arcane-circle-migration.log"
          python3 tools/fix_v0121_alpha9_robe_quad.py
          python3 tools/test_v0121_alpha9_hud_gear_kinetics.py
          git diff --check

      - name: Build Java 25 runtime
        working-directory: ${{ env.PROJECT_DIR }}
        shell: bash
        run: |
          chmod +x gradlew
          set -o pipefail
          ./gradlew --no-daemon --no-configuration-cache clean build --stacktrace --console=plain 2>&1 | tee "$GITHUB_WORKSPACE/arcane-circle-gradle.log"

      - name: Verify alpha.9 JAR contracts
        working-directory: ${{ env.PROJECT_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          test -f "build/libs/$EXPECTED_JAR"
          python3 tools/verify_jar.py "build/libs/$EXPECTED_JAR"
          python3 - <<'PY'
          from pathlib import Path
          import zipfile

          jar = Path('build/libs/arcanecircle-0.12.1-alpha.9.jar')
          with zipfile.ZipFile(jar) as z:
              names = set(z.namelist())
              required = {
                  'kr/moonseungjun/arcanecircle/client/ArcaneWorldMesh.class',
                  'kr/moonseungjun/arcanecircle/client/ArcaneGearRenderer.class',
                  'kr/moonseungjun/arcanecircle/client/MageGearTooltip.class',
                  'kr/moonseungjun/arcanecircle/magic/ArcaneVitalityService.class',
                  'kr/moonseungjun/arcanecircle/magic/SpellArchetype.class',
                  'kr/moonseungjun/arcanecircle/magic/SpellArchetype$Mode.class',
                  'kr/moonseungjun/arcanecircle/magic/SpellKineticsService.class',
                  'kr/moonseungjun/arcanecircle/magic/SpellKineticsService$PendingCast.class',
                  'assets/arcanecircle/textures/item/mage_robe.png',
                  'assets/arcanecircle/textures/item/sage_robe.png',
                  'assets/arcanecircle/textures/item/archmage_robe.png',
              }
              missing = required - names
              if missing:
                  raise SystemExit(f'missing alpha.9 runtime entries: {sorted(missing)}')

              hud = z.read('kr/moonseungjun/arcanecircle/client/ArcaneHud.class')
              for token in (b'absorption', b'System', b'drawHealth'):
                  if token not in hud:
                      raise SystemExit(f'compact animated health HUD missing: {token!r}')

              mesh = z.read('kr/moonseungjun/arcanecircle/client/ArcaneWorldMesh.class')
              for token in (b'submitPass', b'setLineWidth', b'shade'):
                  if token not in mesh:
                      raise SystemExit(f'thick magic mesh missing: {token!r}')

              kinetics = z.read('kr/moonseungjun/arcanecircle/magic/SpellKineticsService.class')
              for token in (b'PROJECTILE', b'CHANNEL', b'FIELD', b'finishKineticCast'):
                  if token not in kinetics:
                      raise SystemExit(f'spell kinetics missing: {token!r}')

              vitality = z.read('kr/moonseungjun/arcanecircle/magic/ArcaneVitalityService.class')
              if b'effectiveAbsorption' not in vitality:
                  raise SystemExit('scaled absorption health missing')

              for class_name in (
                  'kr/moonseungjun/arcanecircle/client/WorldMagicTracker.class',
                  'kr/moonseungjun/arcanecircle/client/SignatureGeometry.class'):
                  bytecode = z.read(class_name)
                  for forbidden in (b'VoxelShape', b'submitShapeOutline', b'Shapes', b'AABB'):
                      if forbidden in bytecode:
                          raise SystemExit(f'pixel-box renderer remains in {class_name}: {forbidden!r}')

              offenders = []
              for name in names:
                  if name.startswith('kr/moonseungjun/arcanecircle/magic/') and name.endswith('.class'):
                      if b'sendParticles' in z.read(name):
                          offenders.append(name)
              if offenders:
                  raise SystemExit(f'particle-centered magic bytecode remains: {offenders}')
          print('Arcane Circle alpha.9 JAR audit: PASS')
          PY
          mkdir -p ../../deliverables
          cp "build/libs/$EXPECTED_JAR" ../../deliverables/
          cp "build/libs/$EXPECTED_JAR.sha256" ../../deliverables/

      - uses: actions/upload-artifact@v4
        with:
          name: arcanecircle-0.12.1-alpha.9
          path: deliverables/
          if-no-files-found: error
          retention-days: 30

      - name: Publish Arcane source and JAR only
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          touch "$PROJECT_DIR/.gitignore"
          grep -qxF '.gradle/' "$PROJECT_DIR/.gitignore" || printf '%s\n' '.gradle/' >> "$PROJECT_DIR/.gitignore"
          grep -qxF 'build/' "$PROJECT_DIR/.gitignore" || printf '%s\n' 'build/' >> "$PROJECT_DIR/.gitignore"
          rm -rf /tmp/arcane-alpha9-source
          mkdir -p /tmp/arcane-alpha9-source
          rsync -a --delete --exclude '/build/' --exclude '/.gradle/' "$PROJECT_DIR/" /tmp/arcane-alpha9-source/
          published=0
          for attempt in 1 2 3 4 5 6; do
            git fetch origin main
            git reset --hard origin/main
            rm -rf "$PROJECT_DIR"
            mkdir -p "$PROJECT_DIR"
            rsync -a /tmp/arcane-alpha9-source/ "$PROJECT_DIR/"
            mkdir -p "$PUBLISHED_DIR"
            rm -f "$PUBLISHED_DIR"/arcanecircle-*.jar "$PUBLISHED_DIR"/arcanecircle-*.jar.sha256
            cp "deliverables/$EXPECTED_JAR" "$PUBLISHED_DIR/$EXPECTED_JAR"
            cp "deliverables/$EXPECTED_JAR.sha256" "$PUBLISHED_DIR/$EXPECTED_JAR.sha256"
            git add -A "$PROJECT_DIR" "$PUBLISHED_DIR"
            if git diff --cached --quiet; then published=1; break; fi
            git commit -m 'Publish Arcane Circle v0.12.1-alpha.9 HUD gear and kinetics [skip ci]'
            if git push origin HEAD:main; then published=1; break; fi
            sleep $((attempt * 2))
          done
          test "$published" -eq 1

      - name: Record result
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/arcane-result
          mkdir -p /tmp/arcane-result
          if test -f "$GITHUB_WORKSPACE/arcane-circle-gradle.log"; then
            cp "$GITHUB_WORKSPACE/arcane-circle-gradle.log" /tmp/arcane-result/latest-gradle.log
          elif test -f "$GITHUB_WORKSPACE/arcane-circle-migration.log"; then
            cp "$GITHUB_WORKSPACE/arcane-circle-migration.log" /tmp/arcane-result/latest-gradle.log
          else
            printf '%s\n' 'No diagnostic log.' > /tmp/arcane-result/latest-gradle.log
          fi
          cat > /tmp/arcane-result/status.txt <<STATUS
          status=${{ job.status }}
          source_sha=$GITHUB_SHA
          run_id=$GITHUB_RUN_ID
          run_url=$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID
          java=25
          gradle=9.2.1
          version=$MOD_VERSION
          STATUS
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          for attempt in 1 2 3 4; do
            git fetch origin main
            git reset --hard origin/main
            rm -rf "$CI_RESULT_DIR"
            mkdir -p "$CI_RESULT_DIR"
            cp -a /tmp/arcane-result/. "$CI_RESULT_DIR/"
            git add -A "$CI_RESULT_DIR"
            if git diff --cached --quiet; then exit 0; fi
            git commit -m "Record Arcane Circle v0.12.1-alpha.9 CI result for ${GITHUB_SHA::7} [skip ci]"
            if git push origin HEAD:main; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
