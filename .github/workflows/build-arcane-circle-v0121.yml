name: Arcane Circle v0.12.1 Maintenance Lock

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Arcane Circle source update in progress'
