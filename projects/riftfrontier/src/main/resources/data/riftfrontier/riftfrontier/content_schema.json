{
  "schemaVersion": 1,
  "status": "m1-active",
  "types": ["combat_archetype", "region", "loot_profile", "creature", "encounter"],
  "packEnvelope": {
    "required": ["schema_version", "pack_id", "definitions"],
    "unknownSchemaPolicy": "reject"
  },
  "validation": {
    "duplicateStableId": "error",
    "missingReference": "error",
    "duplicateSetValue": "error",
    "missingWorldConsequence": "warn"
  },
  "note": "Definitions are decoded into typed CoreDefinition records, registered through ContentRegistry, then reference-validated before runtime use."
}
